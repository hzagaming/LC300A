# 安全策略

## 报告漏洞

请通过仓库维护者提供的私密安全报告渠道提交漏洞。公开渠道尚未配置前，不要在公开 Issue 中披露可利用细节、凭据或个人数据。

## 安全基线

- 普通用户与 root 分离，禁止 root 图形登录。
- 图形应用不得以 root 常驻；特权操作通过最小权限服务和 polkit 授权。
- 默认不启用远程登录服务，默认启用 AppArmor 和基础防火墙策略。
- D-Bus、命令行和配置输入必须进行类型、范围和身份验证。
- 软件包、仓库元数据和发布产物必须验证签名或哈希。
- 遥测默认关闭；诊断报告必须在导出前提示隐私风险。
- 构建和运行时不得保存密码、令牌、私钥或真实用户状态到仓库与发布产物。
- `make lint` 检测常见私钥、云访问密钥、GitHub Token、环境文件和运行时缓存；它是基础防线，不能替代发布前的专用密钥扫描。

## 初始威胁模型

| 资产 | 主要威胁 | 初始控制措施 |
| --- | --- | --- |
| 构建产物 | 依赖或构建环境被篡改 | 固定发行版套件、记录清单、生成 SHA-256、CI 隔离 |
| 更新链路 | 恶意软件包或降级攻击 | APT 签名、版本校验、单实例包管理、明确失败 |
| 特权服务 | 未授权调用或参数注入 | polkit、D-Bus 调用者校验、参数白名单、最小权限 |
| 用户数据 | 日志、报告或遥测泄露 | 默认不上传、导出前告知、日志脱敏 |
| Live/安装环境 | 默认凭据被带入已安装系统 | Live 用户与正式用户隔离、安装时创建新账户 |

具体威胁模型会随阶段 1 的构建与启动链实现同步细化。
